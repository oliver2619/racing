import { IconImgDirective } from './icon-img.directive';

describe('IconImgDirective', () => {
  it('should create an instance', () => {
    const directive = new IconImgDirective();
    expect(directive).toBeTruthy();
  });
});
